name: Index docs → Supabase RAG

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      # Danh bạ nguồn lực nằm ngoài docs/ (xem ghi chú trong scripts/index-docs.js) — thiếu dòng
      # này thì sửa danh bạ không kích hoạt nạp lại kho tri thức.
      - 'nguon-luc/**'

jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Index documents into Supabase pgvector
        run: node scripts/index-docs.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
